import { defineField, defineType } from "sanity";

export const sobreBancoAlimentos = defineType({
    name: 'bancodealimentos_sobre',
    title: 'Banco de Alimentos (sobre)',
    type: 'document',
    fields: [
        defineField({
            name: '',
            title: '',
            type: '',
        }),
    ]
});